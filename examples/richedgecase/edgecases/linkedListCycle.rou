// Linked List Cycle Detection (Floyd's Tortoise and Hare)

import stdlib io as io
import stdlib array as arr
import stdlib string as str



struct ListNode {
    val, next
}

function hasCycle(head) {
    if (head == null) { return false }
    
    slow = head
    fast = head.next
    
    while (fast != null) {
        if (fast.next == null) { return false }
        if (slow == fast) { return true }
        slow = slow.next
        fast = fast.next.next
    }
    return false
}

function testCycleDetection() {
    io.print("--- Testing List Cycle ---")
    
    // List 1: Linear
    l1 = ListNode(1, ListNode(2, ListNode(3, null)))
    io.print("L1 has cycle: " + toString(hasCycle(l1)))
    if (hasCycle(l1)) { io.print("FATAL: Cycle detection false positive") }
    
    // List 2: Cyclic
    head = ListNode(10, null)
    node2 = ListNode(20, null)
    node3 = ListNode(30, null)
    
    head.next = node2
    node2.next = node3
    node3.next = head // Loop back
    
    io.print("L2 has cycle: " + toString(hasCycle(head)))
    if (!hasCycle(head)) { io.print("FATAL: Cycle detection failed to find real cycle") }
}

function main() {
    testCycleDetection()
}

main()
