// Struct Chaining & References - Roucaarize

struct Node {
    val, left, right
}

function testStructChaining() {
    root = Node("root", nil, nil)
    root.left = Node("left", nil, nil)
    root.left.left = Node("left.left", nil, nil)
    root.left.left.right = Node("deep target", nil, nil)

    print("Deep Struct Evaluation: " + root.left.left.right.val)
    if (root.left.left.right.val != "deep target") { print("FATAL: Struct depth evaluation failed!") }
}

function main() {
    testStructChaining()
}

main()
